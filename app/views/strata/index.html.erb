<%= link_to '<< Data Portal Home', controller: :home, acton: :index %>
<h3>Stratum Data</h3>
<p> The number of possible primary sample units per stratum. To download select
  a year and region, then click download. Available as comma-separated values files (csv).
</p>
<%= form_tag strata_path(format: 'csv'),  method: "get",
target: '_blank', class: "data-form form-inline" do %>
  <div class="form-group">
    <%= label_tag :region, "Region" %>
    <%= select_tag :region, nil, id: :region, class: 'form-control' %>
    <%= hidden_field_tag :region, nil, {id: "hidden-region"} %>
    <%= label_tag :year, "Year" %>
    <%= select_tag :year, nil, id: :year, class: 'form-control'%>
    <%= hidden_field_tag :year, nil, {id: "hidden-year"} %>
  </div>
  <%= submit_tag 'Download', id: "download-btn", class: 'btn btn-default' %>
<% end %>
<%= button_tag 'Reset', id: 'reset-btn', class: 'btn btn-default' %>

<h3>Variables</h3>
<ol>
  <li>REGION: A code indicating the region. DRY TORT: Dry Tortugas,
  FLA KEYS: Florida Keys, and SEFCRI: Southeast
  Peninsular Florida.</li>
  <li>YEAR: A number indicating the calendar year.</li>
  <li>PROT: A boolean indicating the protected status: 1 - protected, 2 - unprotected.</li>
  <li>STRAT: A code indicating the stratum. Differs by region.</li>
  <li>NTOT: The number of possible primary sample units for a given year, region,
  stratum, and protected status.</li>
  <li>GRID_SIZE: The length (in meters) to a side of a primary sample unit for
    a given year, region, stratum, and protected status.</li>
  <li>STAGE_LEVEL: Number indicating if survey was conducted using a single-stage or two-stage design. <li>
</ol>
<%= content_tag :div, id: "domains-data", style: {display: 'none'}, data: {domains: @domains} do %>
<% end %>
